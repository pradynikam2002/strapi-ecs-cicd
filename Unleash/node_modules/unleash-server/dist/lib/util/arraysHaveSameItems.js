export const arraysHaveSameItems = (a, b) => {
    const setA = new Set(a);
    const setB = new Set(b);
    if (setA.size !== setB.size) {
        return false;
    }
    return [...setA].every((itemA) => {
        return setB.has(itemA);
    });
};
//# sourceMappingURL=arraysHaveSameItems.js.map