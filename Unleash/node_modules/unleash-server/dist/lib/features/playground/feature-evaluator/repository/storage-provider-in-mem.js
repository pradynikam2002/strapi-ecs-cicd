export default class InMemStorageProvider {
    constructor() {
        this.store = new Map();
    }
    async set(key, data) {
        this.store.set(key, data);
        return Promise.resolve();
    }
    async get(key) {
        return Promise.resolve(this.store.get(key));
    }
}
//# sourceMappingURL=storage-provider-in-mem.js.map