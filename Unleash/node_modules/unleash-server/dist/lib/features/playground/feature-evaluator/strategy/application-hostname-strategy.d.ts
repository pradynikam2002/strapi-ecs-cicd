import { Strategy } from './strategy.js';
export default class ApplicationHostnameStrategy extends Strategy {
    private hostname;
    constructor();
    isEnabled(parameters: {
        hostNames: string;
    }): boolean;
}
//# sourceMappingURL=application-hostname-strategy.d.ts.map