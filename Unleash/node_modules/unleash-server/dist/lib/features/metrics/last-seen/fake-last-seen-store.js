export class FakeLastSeenStore {
    setLastSeen(data) {
        data.map((lastSeen) => lastSeen);
        return Promise.resolve();
    }
    cleanLastSeen() {
        return Promise.resolve();
    }
}
//# sourceMappingURL=fake-last-seen-store.js.map